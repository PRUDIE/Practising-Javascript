const kelvin = 298
//const is used to declare variables that can't be changed
let celsius;
//creating a variable called celsius and then assigning it parameters. kelvin to celsius conversion
celsius = kelvin -273
let fahrenheit
//this line explains the conversion to fahrenheit
fahrenheit = celsius * (9/5) + 32
//print to the least whole number
console.log(Math.floor(fahrenheit))
console.log('The temperature is TEMPERATURE degrees Fahrenheit' )

